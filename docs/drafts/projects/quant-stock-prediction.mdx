---
slug: quant-stock-prediction
title: "Quantitative Stock-Prediction Pipeline"
oneLiner: "An end-to-end time-series forecasting pipeline — data ingestion through LSTM training and evaluation — reaching 1.268% MAPE." # [CONFIRM framing — see warning below]
category: ml-cv
tags: [LSTM, Time series, Python] # [CONFIRM: PyTorch or TensorFlow, data sources]
timeframe: "" # [CONFIRM]
role: "Solo"
repo: "" # [CONFIRM: repo link, if public]
featured: false
metrics:
  - { label: "MAPE", value: "1.268%" } # [CONFIRM: horizon, target variable, ticker set]
---

> **⚠️ Framing check before this goes public.** A low MAPE on *price levels* is the classic
> trap in this genre: prices are near random-walk, so next-day price prediction gets very
> low MAPE from persistence alone, and quant-literate readers know it. To make this credible,
> confirm: (1) prediction horizon and target (price? return?), (2) how it compares to a
> naive last-value baseline, (3) train/test methodology (walk-forward?). If the honest answer
> is "it barely beats naive," the strong move is to *say so* — an engineer who measures
> against the right baseline and reports it reads far better than an inflated headline number.

## Problem

Build a complete forecasting pipeline for equity time series — not a notebook that fits one model to one CSV, but the full path: data ingestion, feature preparation, sequence modeling, and honest evaluation. [CONFIRM: what motivated it and what universe of tickers it covered.]

## Constraints

- Financial time series are non-stationary and low signal-to-noise — the modeling problem is mostly an evaluation-discipline problem.
- [CONFIRM: data source and any rate/coverage limits that shaped the design.]

## Approach

An LSTM over windowed sequences. [CONFIRM: features beyond raw price — returns, volume, technical indicators? Window length, forecast horizon, and how the train/validation/test split respected time ordering.]

## Why it's technically hard

The hard part of financial ML is not the network; it's not fooling yourself. Leakage hides everywhere — normalization fitted on the full series, shuffled splits, targets that peek forward — and every one of them manufactures fake accuracy. The pipeline's value is the discipline: time-ordered splits, [CONFIRM: walk-forward evaluation?], and metrics compared against a naive baseline rather than reported in a vacuum.

## Result

1.268% MAPE on [CONFIRM: horizon/target/test period], versus [CONFIRM: naive baseline number — this comparison is what makes the metric meaningful].

## What I'd do next

[CONFIRM — candidates: predict returns instead of prices, compare against gradient-boosted and linear baselines, add transaction-cost-aware backtesting if it ever drives decisions.]
