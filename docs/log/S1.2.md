# S1.2 — Landing / hero

Status: done · Started: 2026-07-08 · Shipped: 2026-07-08

## Ideate

Three calls made up front, each reversible:

1. **Positioning line** drafted and shipped (alternates in
   docs/drafts/REVIEW.md for Shao to swap). Chosen: "I work where software
   meets hardware — control loops on microcontrollers, autonomous robots,
   and the ML systems that let them see."
2. **Motion:** CSS-only fade-up keyframes instead of Framer Motion — the
   entrance animation doesn't need orchestration, and ~35 kB of JS is a bad
   trade against the perf budget. Framer joins when a story genuinely needs
   it. (Deviation from the plan's stack table, reasoned.)
3. **Featured strip** renders from an interim typed module
   (`src/content/data/projects.ts`, confirmed facts only, no links since
   detail pages don't exist until S3.x). S2.1's Zod layer supersedes it.

## Acceptance deviations

- "Primary links (GitHub/LinkedIn/email/resume)": GitHub only — LinkedIn/
  email/resume are unconfirmed facts (F4 in STORIES.md). They join the hero
  when Shao provides them; inventing them would violate content rules.

## LCP note

h1 is the LCP element and is never animated (opacity-from-0 on the LCP
delays it). Secondary elements stagger in at 80/160/240 ms. Perf dipped to
exactly 95 because the Space Grotesk swap re-triggers the LCP entry under
mobile throttling; taking JetBrains Mono off preload (it only styles small
accents) bought it back to 97. Deeper font strategy → S7.2.

## Gotchas

- ~/Documents cloud sync duplicated build artifacts (`routes.d 2.ts` etc.)
  inside `.next/`, breaking typecheck with duplicate declarations. Fixed
  with `rm -rf .next out`; noted in CLAUDE.local.md.
- Playwright strict mode: nav's aria-label "GitHub" collides with the hero
  "GitHub ↗" link under a `/GitHub/` name matcher — locators must be exact.

## Validate

- typecheck ✅ · lint ✅ · format:check ✅ · build (export) ✅
- Playwright 14/14 — including F3 (nav hides on scroll down / reveals on
  scroll up) now that the landing scrolls
- Lighthouse 97 / 100 / 100 / 100 — budgets met

## Evaluate

- 375 (light) and 1440 (dark) verified in preview; theme-override
  precedence confirmed live (stored choice beats system emulation — by
  design). No console output.
- F3 resolved; F4 (socials/resume) still open on Shao.
