# S5.1 — Coursework section with Fast Robots done well

Status: done · Started: 2026-07-12 · Shipped: 2026-07-12

## Decisions

- /coursework is the course-centric view (reports, series) alongside the
  project-centric /projects — kept per plan despite partial overlap with
  the School-work section.
- Fast Robots entry: context, techniques, prominent outbound link to the
  report site, internal case-study link. **No iframe** (pinned by test).
- ECE 6750 entry seeds the "collection under one tab" idea (F5): course
  card with SpGEMM as the first written-up project.
- F1 (migrate reports to native MDX later) already tracked in STORIES.md.

## Side effects fixed in this pass

- Third nav link overflowed a 375px viewport, burying the theme toggle
  under the Coursework link (caught by a hanging mobile test — the click
  couldn't land). Nav now uses a disclosure menu below `sm`:
  wordmark + toggle + hamburger; links + GitHub stack in an
  `aria-expanded`-tracked panel.
- A stray `~/pnpm-lock.yaml` made Next infer `$HOME` as the workspace
  root, ballooning turbopack builds until webServer timeouts; pinned
  `turbopack.root` in next.config.ts. (Stray home-dir package files left
  in place — they're Shao's.)

## Validate (combined run for S3.2 partial + S4.2 partial + S5.1)

- typecheck ✅ · lint ✅ · format ✅ · build (export) ✅
- Playwright 32/32 (new: coursework links + no-iframe, timelines,
  individual-track render, prev/next, mobile menu semantics)
- Lighthouse 97 / 100 / 100 / 100

## Evaluate

- Mobile menu verified interactively at 375 dark (panel, X toggle,
  targets); /coursework + /projects verified against the static export.
