# S6.1 — Contact + links

Status: done · Started: 2026-07-12 · Shipped: 2026-07-12

## Decisions (Shao, 2026-07-12)

- **mailto only** (no form service) — a form can be added later without
  rework.
- Public email: shaostassen225@gmail.com, obfuscated.
- LinkedIn confirmed: linkedin.com/in/shaostassen → contact page, footer,
  and hero (F4 resolved except resume.pdf).

## Implementation

- `src/content/data/profile.ts` — single source for identity links; email
  stored as user/domain parts.
- `ContactEmail` (client): address + mailto assembled only after
  hydration; the prerendered HTML contains neither the address nor any
  `mailto:` — pinned by a test that greps the raw response. Copy button
  with clipboard fallback (address is selectable text regardless).
- Spam resistance = markup-scanning harvesters get nothing; humans get a
  one-click mailto and a copy button. No-JS visitors still get GitHub and
  LinkedIn links.

## Validate

- typecheck ✅ (one transient exit-1: tsc raced the test build
  regenerating .next/types; clean on rerun) · lint ✅ · format ✅
- Playwright 34/34 · Lighthouse 97 / 100 / 100 / 100

## Evaluate

- /contact verified against the static export (raw-HTML grep = 0 matches
  for the address; hydrated page shows link + copy + socials).
